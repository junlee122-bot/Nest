# DECISIONS — v3 업그레이드 설계 결정 기록

> 최종 업그레이드 프롬프트 v3 실행에 앞서, §0.5(디자인 자율권)·§6-6(토큰 계획+자기비판)에 따라
> 방향을 여기에 고정한다. 이후 Phase(P1~P7)는 이 문서를 기준으로 진행.

## P0. 디자인 방향 — "둥이의 현관(玄關)"

### 컨셉 한 줄
차가운 텔레콤 대시보드가 아니라, **집에 들어오면 둥이(마스코트)가 맞아주는 따뜻한 현관**.
정확성·법적 근거라는 딱딱한 코어를, 캐릭터와 온기 있는 뉴트럴로 감싼다.

### 색 토큰 (4~6개 축, KT 레드 유지)
| 토큰 | 값 | 역할 |
|---|---|---|
| `brand` | `#E6002D` (hover `#C20026`, tint `#FDEAEE`) | 유지 — 로고·CTA·강조. KT 아이덴티티 |
| `ink` | `#1A1A1A` | 유지 — 본문·헤더 |
| `bg` / `card` / `line` / `muted` | 기존 값 유지 | 클린 뉴트럴 기반 |
| **`straw`** (신규) | `DEFAULT #A97E50` / `tint #F7F0E6` / `deep #6B4F30` | **둥지 결(짚·잔가지) 웜 액센트** — 마스코트 존·홈 히어로·둥지 아크 전용. 카드 배경엔 쓰지 않음(빵집 브랜드화 방지) |
| `warn` / `danger` / `ok` | 기존 값 유지 | 안전 시맨틱 — 불변 원칙(안전 배너에 브랜드 레드 금지) |

### 타이포 (2역할)
- **Display**: Pretendard `font-extrabold tracking-tight` — 히어로·섹션 제목·판정 헤드라인
- **Body**: Pretendard `15px/1.6` — 본문·설명. (폰트 추가 없음 — 스택 동결 준수)

### 레이아웃 컨셉
- 모바일 퍼스트 단일 컬럼(`max-w-app`) 유지 — 검증된 뼈대는 재작성하지 않는다(가드레일).
- 홈 = **현관**: 둥이 인사 + 말풍선("오늘 집에 무슨 일 있어요?") + 메인 CTA 1개, 기능은 "방(rooms)" 리스트로.
- 기능 페이지 = **방**: 헤더에 둥이 소형 등장(mood 연동), 결과 카드에 등장 모션.

### 시그니처 1개 — **둥지 아크(Nest Arc)**
로고의 둥지 그릇 곡선(`M5 29c0 7.5 8.5 13 19 13s19-5.5 19-13`)을 시스템 전체의 시그니처로 승격:
1. 섹션 제목 밑줄 액센트(짚색 스트로크)
2. 로딩 인디케이터 — 아크가 그려지는 draw 애니메이션(스피너 대체)
3. 둥이의 받침(perch) — 마스코트가 항상 둥지 위에 앉음
4. 홈 히어로 하단 경계 곡선
구현: `components/NestArc.tsx` (SVG, currentColor) + framer-motion draw.

### AI 디폴트 룩 3종 회피 점검
1. **보라·블루 그라데이션/글래스모피즘** → 없음. 웜 뉴트럴(straw) + KT 레드.
2. **이모지 타일 그리드 홈** → 현재 홈이 이 패턴. P4에서 둥이 현관 + 커스텀 미니 씬(둥지 아크 배경 위 라인 아이콘)으로 교체.
3. **범용 SaaS 히어로(중앙 헤드라인+CTA 2개+3열 카드)** → 대화형 현관(말풍선+단일 CTA) 구조로 대체.

### 자기비판 (§6-6)
- ⚠ 짚색+레드 = 베이커리처럼 보일 위험 → straw는 마스코트 존·아크·히어로 배경에만. 카드·버튼은 화이트/레드 유지.
- ⚠ 캐릭터 과다 노출 = 유치함/신뢰 하락 위험 → 결과 카드(법적 판단·안전 경고) 영역에는 둥이를 넣지 않는다. 판정·disclaimer 영역은 지금의 절제된 톤 유지.
- ⚠ 모션 과다 = 산만+성능 → LazyMotion(domAnimation)만, 등장 모션은 120~360ms, `useReducedMotion` 전 분기. 무한 반복 모션은 로딩·둥이 idle 숨쉬기 한정.
- Figma·외부 레퍼런스 조사(§0.5 B·C)는 "가능하면" 항목 — 컨테이너 네트워크 제약으로 생략하고, 대신 시안 자기비평(스크린샷 검토)을 P4에서 수행.

## P0. 실행 계획 (Phase 순서)
- **P1 토대**: `framer-motion` 설치, `lib/motion.ts`(variants + reduced-motion), 회귀 없음 확인
- **P2 둥이**: `components/mascot/Doongi.tsx` — 6 mood(hello/thinking/found/warning/cheer/sleepy), SVG+모션, NestMark와 혈통 유지
- **P3 연결**: 로딩=thinking, 결과=found/warning, 완료 액션=cheer, 온보딩=hello 등 상태 연동 + 결과 카드 stagger 등장
- **P4 리디자인**: 홈 현관화, IntroSplash(첫 방문 1회·스킵 가능), NestArc 시그니처 적용, 기능 페이지 헤더 통일
- **P5 공공데이터**: `lib/integrations/` — ① 법제처 law API(법령 원문 대조 자동화) ② 국토부 전월세 실거래가 ③ 기상청 초단기실황(계절 맥락 강화) ④ 건축물대장 ⑤ Daum 주소검색 ⑥ 카카오 로컬 ⑦ 공휴일. **전부 키 없으면 기존 폴백으로 무손실 동작**, 결과엔 "참고용·법적 효력 없음" 고지
- **P6 응답 튜닝**: few-shot 예시, JSON 스키마 검증+1회 재시도, 모델 라우팅(`ANTHROPIC_MODEL_TEXT`), 프롬프트 캐싱, temperature 명시, `evals/` 하네스, PROMPTS.md 동기화
- **P7(선택)**: 둥지 키우기 — localStorage 기반 성장(무DB·무PII 불변)

## 불변 원칙 (v3 §고정)
정확성·법적 근거·disclaimer·안전 에스컬레이션 유지 / 무로그인·무DB·무PII / 키 커밋 금지 /
API는 서버 전용+폴백 필수 / 기존 6기능 파괴 금지 / Next 메이저 업그레이드 금지 /
framer-motion 외 애니메이션 라이브러리 금지 / prefers-reduced-motion 존중 / KT 레드 유지

---

## v4. 디자인 전면 리뉴얼 — "민트 가든" (사용자 지시, 2026-07-02)

v3의 KT 레드 유지 원칙은 **사용자 지시로 해제**됨("완전히 새로운 팔레트 + 플레이풀 캐릭터 중심",
공모전 교육자료 레퍼런스 무드). 결정:

- **팔레트**: 새싹 그린(#45A567) 프라이머리 + 앰버·코랄·스카이·짚색 파스텔 로테이션.
  민트에 한정하지 않는 멀티컬러(사용자 추가 지시) — 페이지·요소마다 톤 정체성 부여
  (수리=그린, 계약=스카이, 시세·행정·장보기=앰버, 공과금=코랄, 주거비=짚색).
- **안전 시맨틱 불변**: 경고 앰버 / 위험 레드(danger)는 브랜드와 분리 유지.
  계약서 위험도 high는 brand→danger로 교정, 공과금 초과는 coral.
- **질감**: 3D 촉감 버튼(누르면 가라앉음), 2px 보더 + 큰 라운드(rounded-3xl) 카드,
  형광펜 하이라이트(.marker), 웜 크림 배경(#FAFAF3) + 민트·앰버 블롭.
- **둥이 v2**: 플랫 파스텔 병아리로 전면 리디자인 — 머리 위 '새싹'이 시그니처
  (브랜드 그린 + 둥지 키우기 성장 테마), 눈웃음·큰 홍조·얇은 웜 아웃라인. 6 mood API 불변.
- **"AI로 만든 티" 제거**(사용자 지시): 가운데 정렬 대칭 히어로 → 왼쪽 정렬 비대칭 +
  기울어진 스티커 말풍선, 홈의 3단계 스트립 삭제(온보딩과 중복), 헤딩 이모지 제거,
  카피의 em-dash(—) 제거·자연어화, 메인 카드만 둥이가 앉은 큰 블록으로 차별화(획일 카드 탈피).

---

## v5. "실제 앱" 리디자인 (사용자 지시, 2026-07-02)

레퍼런스 수준으로 각 화면을 앱답게. 결정:

- **내비게이션**: 하단 탭바(홈·집수리·계약서·시세·전체) 전역 고정 + 기능 페이지는
  sticky 앱바(뒤로가기+타이틀)로 통일. 수리 결과 액션바는 탭바 위에 스택.
- **팔레트 리터치**: 크림+2px보더+3D버튼(게임 감각) → 뉴트럴 그레이지 배경(#F6F7F6),
  화이트 카드+헤어라인+소프트 섀도, 딥 프레시 그린(#0DA05C) 원포인트.
  톤 로테이션(sun·coral·sky·straw)은 아이콘 틴트로 절제 사용. 오로라는 스플래시 전용.
- **타이포 절제**: Gmarket Sans는 큰 타이틀(h1)만, 카드·리스트 제목은 Pretendard bold.
- **홈 전용 제작**: 로고 바 + 시간대 인사 + 둥이 + 메인 CTA + 퀵액션 4타일 +
  내 둥지 + 생활 도구 리스트. 상세 면책 고지는 '전체(/menu)' 페이지로 이동(홈은 1줄+링크).
- **/menu 신설**: 전체 기능·내 둥지·최근 기록·이용 안내(면책 전문)·출품 정보.
- 기능 데이터 단일 소스 lib/features.ts (홈·전체·탭바 공유).

### v5.1 디테일 패스 (2026-07-02)
- 탭바: layoutId 공유 인디케이터(스프링 슬라이드) — LazyMotion domAnimation → domMax 승격
- 페이지 전환: app/template.tsx 페이드(opacity만 — transform은 fixed 요소를 가두므로 금지)
- 시즌 뱃지: lib/season.ts (여름 장마=수리, 겨울 한파=수리, 봄=계약, 가을=시세) → 홈 퀵액션 코랄 뱃지+안내줄, hydration 안전하게 마운트 후 표시
- 계약서: 둥이 헤럴드·로딩(thinking), 위험 인용문 좌측 보더 시맨틱(레드/앰버), 종합 '높음' 배지 danger
- 장보기: 세그먼트 탭, 요일 식단 이모지→아이콘, 둥이 로딩·sleepy 빈 상태
- 정중/단호 토글 세그먼트화, 최근 기록 이모지→톤 아이콘, 사진 업로더·칩 선택 상태 v5 정합

### v5.2 UX 디테일 패스 (2026-07-02)
- iOS 자동 확대 방지: 모든 폼 컨트롤 16px(text-base)
- 되묻기 카드에 직접 입력 폼 추가(칩에 없는 답), 후속질문 칩은 맥락 없을 때(예시·기록 복원) 숨김
- 계약서: 9,000자부터 글자수 카운터(한도 초과 시 경고), 로딩 중 예시 버튼 비활성
- 장보기: 예산 퀵 칩(2·3·5·7만원)
- 시세: 조회 로딩 둥이, 결과 헤더에 주택 유형 라벨
- 접근성: 온보딩 ESC 닫기, 스플래시 아무 키 스킵, 탭바 focus-visible 링, CSS 애니메이션 reduced-motion 정지
- 앱 감각: tap-highlight 제거, ::selection 민트, 앱바 노치 safe-area, viewportFit cover
- 앱 아이콘(icon.svg)을 구 KT 레드 → 새 아이덴티티(그린+새싹 병아리)로 교체
