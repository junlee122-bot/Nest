# DECISIONS — v3 업그레이드 설계 결정 기록

> 최종 업그레이드 프롬프트 v3 실행에 앞서, §0.5(디자인 자율권)·§6-6(토큰 계획+자기비판)에 따라
> 방향을 여기에 고정한다. 이후 Phase(P1~P7)는 이 문서를 기준으로 진행.

## P0. 디자인 방향 — "둥이의 현관(玄關)"

### 컨셉 한 줄
차가운 텔레콤 대시보드가 아니라, **집에 들어오면 둥이(마스코트)가 맞아주는 따뜻한 현관**.
정확성·법적 근거라는 딱딱한 코어를, 캐릭터와 온기 있는 뉴트럴로 감싼다.

### 색 토큰 (4~6개 축, KT 레드 유지)
| 토큰 | 값 | 역할 |
|---|---|---|
| `brand` | `#E6002D` (hover `#C20026`, tint `#FDEAEE`) | 유지 — 로고·CTA·강조. KT 아이덴티티 |
| `ink` | `#1A1A1A` | 유지 — 본문·헤더 |
| `bg` / `card` / `line` / `muted` | 기존 값 유지 | 클린 뉴트럴 기반 |
| **`straw`** (신규) | `DEFAULT #A97E50` / `tint #F7F0E6` / `deep #6B4F30` | **둥지 결(짚·잔가지) 웜 액센트** — 마스코트 존·홈 히어로·둥지 아크 전용. 카드 배경엔 쓰지 않음(빵집 브랜드화 방지) |
| `warn` / `danger` / `ok` | 기존 값 유지 | 안전 시맨틱 — 불변 원칙(안전 배너에 브랜드 레드 금지) |

### 타이포 (2역할)
- **Display**: Pretendard `font-extrabold tracking-tight` — 히어로·섹션 제목·판정 헤드라인
- **Body**: Pretendard `15px/1.6` — 본문·설명. (폰트 추가 없음 — 스택 동결 준수)

### 레이아웃 컨셉
- 모바일 퍼스트 단일 컬럼(`max-w-app`) 유지 — 검증된 뼈대는 재작성하지 않는다(가드레일).
- 홈 = **현관**: 둥이 인사 + 말풍선("오늘 집에 무슨 일 있어요?") + 메인 CTA 1개, 기능은 "방(rooms)" 리스트로.
- 기능 페이지 = **방**: 헤더에 둥이 소형 등장(mood 연동), 결과 카드에 등장 모션.

### 시그니처 1개 — **둥지 아크(Nest Arc)**
로고의 둥지 그릇 곡선(`M5 29c0 7.5 8.5 13 19 13s19-5.5 19-13`)을 시스템 전체의 시그니처로 승격:
1. 섹션 제목 밑줄 액센트(짚색 스트로크)
2. 로딩 인디케이터 — 아크가 그려지는 draw 애니메이션(스피너 대체)
3. 둥이의 받침(perch) — 마스코트가 항상 둥지 위에 앉음
4. 홈 히어로 하단 경계 곡선
구현: `components/NestArc.tsx` (SVG, currentColor) + framer-motion draw.

### AI 디폴트 룩 3종 회피 점검
1. **보라·블루 그라데이션/글래스모피즘** → 없음. 웜 뉴트럴(straw) + KT 레드.
2. **이모지 타일 그리드 홈** → 현재 홈이 이 패턴. P4에서 둥이 현관 + 커스텀 미니 씬(둥지 아크 배경 위 라인 아이콘)으로 교체.
3. **범용 SaaS 히어로(중앙 헤드라인+CTA 2개+3열 카드)** → 대화형 현관(말풍선+단일 CTA) 구조로 대체.

### 자기비판 (§6-6)
- ⚠ 짚색+레드 = 베이커리처럼 보일 위험 → straw는 마스코트 존·아크·히어로 배경에만. 카드·버튼은 화이트/레드 유지.
- ⚠ 캐릭터 과다 노출 = 유치함/신뢰 하락 위험 → 결과 카드(법적 판단·안전 경고) 영역에는 둥이를 넣지 않는다. 판정·disclaimer 영역은 지금의 절제된 톤 유지.
- ⚠ 모션 과다 = 산만+성능 → LazyMotion(domAnimation)만, 등장 모션은 120~360ms, `useReducedMotion` 전 분기. 무한 반복 모션은 로딩·둥이 idle 숨쉬기 한정.
- Figma·외부 레퍼런스 조사(§0.5 B·C)는 "가능하면" 항목 — 컨테이너 네트워크 제약으로 생략하고, 대신 시안 자기비평(스크린샷 검토)을 P4에서 수행.

## P0. 실행 계획 (Phase 순서)
- **P1 토대**: `framer-motion` 설치, `lib/motion.ts`(variants + reduced-motion), 회귀 없음 확인
- **P2 둥이**: `components/mascot/Doongi.tsx` — 6 mood(hello/thinking/found/warning/cheer/sleepy), SVG+모션, NestMark와 혈통 유지
- **P3 연결**: 로딩=thinking, 결과=found/warning, 완료 액션=cheer, 온보딩=hello 등 상태 연동 + 결과 카드 stagger 등장
- **P4 리디자인**: 홈 현관화, IntroSplash(첫 방문 1회·스킵 가능), NestArc 시그니처 적용, 기능 페이지 헤더 통일
- **P5 공공데이터**: `lib/integrations/` — ① 법제처 law API(법령 원문 대조 자동화) ② 국토부 전월세 실거래가 ③ 기상청 초단기실황(계절 맥락 강화) ④ 건축물대장 ⑤ Daum 주소검색 ⑥ 카카오 로컬 ⑦ 공휴일. **전부 키 없으면 기존 폴백으로 무손실 동작**, 결과엔 "참고용·법적 효력 없음" 고지
- **P6 응답 튜닝**: few-shot 예시, JSON 스키마 검증+1회 재시도, 모델 라우팅(`ANTHROPIC_MODEL_TEXT`), 프롬프트 캐싱, temperature 명시, `evals/` 하네스, PROMPTS.md 동기화
- **P7(선택)**: 둥지 키우기 — localStorage 기반 성장(무DB·무PII 불변)

## 불변 원칙 (v3 §고정)
정확성·법적 근거·disclaimer·안전 에스컬레이션 유지 / 무로그인·무DB·무PII / 키 커밋 금지 /
API는 서버 전용+폴백 필수 / 기존 6기능 파괴 금지 / Next 메이저 업그레이드 금지 /
framer-motion 외 애니메이션 라이브러리 금지 / prefers-reduced-motion 존중 / KT 레드 유지

---

## v4. 디자인 전면 리뉴얼 — "민트 가든" (사용자 지시, 2026-07-02)

v3의 KT 레드 유지 원칙은 **사용자 지시로 해제**됨("완전히 새로운 팔레트 + 플레이풀 캐릭터 중심",
공모전 교육자료 레퍼런스 무드). 결정:

- **팔레트**: 새싹 그린(#45A567) 프라이머리 + 앰버·코랄·스카이·짚색 파스텔 로테이션.
  민트에 한정하지 않는 멀티컬러(사용자 추가 지시) — 페이지·요소마다 톤 정체성 부여
  (수리=그린, 계약=스카이, 시세·행정·장보기=앰버, 공과금=코랄, 주거비=짚색).
- **안전 시맨틱 불변**: 경고 앰버 / 위험 레드(danger)는 브랜드와 분리 유지.
  계약서 위험도 high는 brand→danger로 교정, 공과금 초과는 coral.
- **질감**: 3D 촉감 버튼(누르면 가라앉음), 2px 보더 + 큰 라운드(rounded-3xl) 카드,
  형광펜 하이라이트(.marker), 웜 크림 배경(#FAFAF3) + 민트·앰버 블롭.
- **둥이 v2**: 플랫 파스텔 병아리로 전면 리디자인 — 머리 위 '새싹'이 시그니처
  (브랜드 그린 + 둥지 키우기 성장 테마), 눈웃음·큰 홍조·얇은 웜 아웃라인. 6 mood API 불변.
- **"AI로 만든 티" 제거**(사용자 지시): 가운데 정렬 대칭 히어로 → 왼쪽 정렬 비대칭 +
  기울어진 스티커 말풍선, 홈의 3단계 스트립 삭제(온보딩과 중복), 헤딩 이모지 제거,
  카피의 em-dash(—) 제거·자연어화, 메인 카드만 둥이가 앉은 큰 블록으로 차별화(획일 카드 탈피).

---

## v5. "실제 앱" 리디자인 (사용자 지시, 2026-07-02)

레퍼런스 수준으로 각 화면을 앱답게. 결정:

- **내비게이션**: 하단 탭바(홈·집수리·계약서·시세·전체) 전역 고정 + 기능 페이지는
  sticky 앱바(뒤로가기+타이틀)로 통일. 수리 결과 액션바는 탭바 위에 스택.
- **팔레트 리터치**: 크림+2px보더+3D버튼(게임 감각) → 뉴트럴 그레이지 배경(#F6F7F6),
  화이트 카드+헤어라인+소프트 섀도, 딥 프레시 그린(#0DA05C) 원포인트.
  톤 로테이션(sun·coral·sky·straw)은 아이콘 틴트로 절제 사용. 오로라는 스플래시 전용.
- **타이포 절제**: Gmarket Sans는 큰 타이틀(h1)만, 카드·리스트 제목은 Pretendard bold.
- **홈 전용 제작**: 로고 바 + 시간대 인사 + 둥이 + 메인 CTA + 퀵액션 4타일 +
  내 둥지 + 생활 도구 리스트. 상세 면책 고지는 '전체(/menu)' 페이지로 이동(홈은 1줄+링크).
- **/menu 신설**: 전체 기능·내 둥지·최근 기록·이용 안내(면책 전문)·출품 정보.
- 기능 데이터 단일 소스 lib/features.ts (홈·전체·탭바 공유).

### v5.1 디테일 패스 (2026-07-02)
- 탭바: layoutId 공유 인디케이터(스프링 슬라이드) — LazyMotion domAnimation → domMax 승격
- 페이지 전환: app/template.tsx 페이드(opacity만 — transform은 fixed 요소를 가두므로 금지)
- 시즌 뱃지: lib/season.ts (여름 장마=수리, 겨울 한파=수리, 봄=계약, 가을=시세) → 홈 퀵액션 코랄 뱃지+안내줄, hydration 안전하게 마운트 후 표시
- 계약서: 둥이 헤럴드·로딩(thinking), 위험 인용문 좌측 보더 시맨틱(레드/앰버), 종합 '높음' 배지 danger
- 장보기: 세그먼트 탭, 요일 식단 이모지→아이콘, 둥이 로딩·sleepy 빈 상태
- 정중/단호 토글 세그먼트화, 최근 기록 이모지→톤 아이콘, 사진 업로더·칩 선택 상태 v5 정합

### v5.2 UX 디테일 패스 (2026-07-02)
- iOS 자동 확대 방지: 모든 폼 컨트롤 16px(text-base)
- 되묻기 카드에 직접 입력 폼 추가(칩에 없는 답), 후속질문 칩은 맥락 없을 때(예시·기록 복원) 숨김
- 계약서: 9,000자부터 글자수 카운터(한도 초과 시 경고), 로딩 중 예시 버튼 비활성
- 장보기: 예산 퀵 칩(2·3·5·7만원)
- 시세: 조회 로딩 둥이, 결과 헤더에 주택 유형 라벨
- 접근성: 온보딩 ESC 닫기, 스플래시 아무 키 스킵, 탭바 focus-visible 링, CSS 애니메이션 reduced-motion 정지
- 앱 감각: tap-highlight 제거, ::selection 민트, 앱바 노치 safe-area, viewportFit cover
- 앱 아이콘(icon.svg)을 구 KT 레드 → 새 아이덴티티(그린+새싹 병아리)로 교체

---

## v6. 장보기 코치 데이터 고도화 (사용자 지시, 2026-07-02)

AI 추측 → 실데이터 접지(grounding). 워크플로(병렬 에이전트)로 데이터 큐레이션·API 스펙 검증.

- **내장 DB 3종(lib/grocery/, 키 불필요)**: 제철 달력(12개월×채소/과일/수산), 보관법 66종
  (별칭 매칭·기한·팁·냉동 가능), 참고 가격표 43품목(서울 소매 범위). 프롬프트 주입 + 서버 후처리
  매칭으로 이중 사용.
- **외부 API 2종(키 없으면 무손실 폴백)**: aT KAMIS 일일 소매가(dailySalesList, 6h 캐시,
  '오늘 시세' 표기+프롬프트 접지), 식약처 COOKRCP01 공공 레시피(보관 기한 최단 재료로 검색,
  '공공 레시피로 더 해먹기' 카드 — 스펙은 공식 sample 키로 라이브 검증).
- **아키텍처 원칙**: AI 출력 스키마와 서버 부착 필드(storage_tip·price_ref·today_price·meta·
  db_recipes·storage_notes)를 분리 — 모델이 형식을 어겨도 데이터 레이어는 안전.
- UI: 제철 칩 카드/제철 뱃지, 항목별 오늘 시세·참고가·보관 한 줄, 가격 출처 각주,
  '먼저 쓸 순서'(기한 오름차순), 공공 레시피 카드(사진·열량·단계).

---

## v7. 외부 리뷰 반영 (2026-07-03) — 정리·신뢰·안전 (기능 추가 아님)

- **P1 홈 단순화**: 주 CTA 1(사진 진단) + 보조 2(계약서·시세) + 생활 도구 강등(공과금·장보기·주거비·행정).
  포지셔닝 "집 문제 생겼을 때 가장 먼저". 탭 라벨 11px 등 텍스트 상향.
- **P2 수리 결과**: 상단 핵심(안전·긴급도·판정·당장 할 것·집주인 문구=기본 열기)만 펼침,
  근거·다음 단계·도움받기 접힘(긴급 연락 변형만 항상 펼침). NextSteps 키를 결과 해시로 분리.
- **P3 장보기 신뢰**: 가격 출처 배지 4종, 한 줄 요약+자세히 펼치기(줄바꿈 허용),
  단가·행사가 고지, 데이터 버전(meta.ts), '목안' 0건, 조리 난이도 3단 세그먼트.
- **P4 견고성**: extractJson 균형 괄호 파서(문자열·이스케이프 인지, 단위 검증),
  validator enum/길이/배열 상한, /api/assist 서버측 이미지·바디 상한, LLM 라우트 IP 레이트리밋(보험),
  eslint-config-next → devDependencies.
- **P5 접근성**: aria-pressed/tablist/aria-selected, label↔input 연결, placeholder-only aria-label,
  사진 업로드 alert→인라인 role=alert.
- **P6 폴리시**: Doongi defs id useId화(다중 렌더 참조 버그), 계약서 헤럴드 마스코트 제거(법률 화면 규칙).
- 지킨 것: 브랜드 그린 유지, Next 14.2.35 고정, 폰트 리팩터·강CSP·전면 공통화 안 함(§9).

### v8. Pexels 음식 참고 이미지 (2026-07-03)
최종 실행 프롬프트가 전제한 foodImages는 레포에 없던 상태 → 스펙 그대로 신규 구현:
- lib/integrations/foodImages.ts — Pexels 검색(locale ko-KR, landscape, 24h 캐시), 키 없으면 null
- /grocery 한정: '남은 재료' AI 레시피 카드에만 서버 부착(recipe.photo, AI 스키마와 분리)
- Pexels 가이드라인 크레딧(작가·원본 링크) + "참고 이미지" 고지, img lazy/decoding=async
- 키 없는 데모: 텍스트 카드 무손실 폴백 확인. 나머지 P1~P6 완료 기준은 v7에서 이행 완료 재검증.

---

## v9. 인터랙티브 쇼케이스 + 예산 대체재 (2026-07-03)

### 쇼케이스 이식 (P1)
- /showcase — 심사위원용 90초 인터랙티브 데모(드롭인 이식). 시나리오 3종(수리·계약서·장보기)
  자동 순환 + 데이터 신호 카드 6종 + Before/After 슬라이더 + 발표용 멘트.
- 진입점: 홈(주 CTA 아래 작은 다크 카드 — 주 CTA를 밀지 않음), 전체 탭 '데모' 섹션.
- AppTabBar에 HIDE_PATHS 도입 — /showcase는 풀스크린(탭바 숨김, -mb-[68px]로 전역 패딩 상쇄).

### 폴리시 (P2)
- 컨트롤: 재생/일시정지(정확한 아이콘) + '처음부터' 분리. 자동 재생은 opt-in(자동 시작 없음).
- 정직성: '실시간' 배지 → '데모 재현', 목업 아래 "심사용 예시" 고지, 신호 카드마다
  실제 폴백 동작 태그(내장·연동·무키 폴백). 미구현 표현 제거.
- 드롭인 버그 수정: Tailwind 불투명도 스케일 밖 값(/8·/12·/72 등)이 CSS 미생성되던 것을
  5단위로 정규화. 한글 break-keep, 장식 요소 aria-hidden.

### C-1. 예산 초과 대체재 추천 (P3)
- lib/grocery/substitutes.ts — 큐레이션 스왑 룰(요리 역할이 실제로 겹치는 조합만,
  참고가격표에 있는 품목만 제안) + parseBudgetWon("3만5천"·"35,000원"·"주당 3만원" 해석,
  해석 불가면 null → 카드 미표시가 안전).
- 서버 후처리(buildBudgetFix): 예산 초과 시 비싼 항목부터 최대 3개 스왑, 절약액은
  대체재 참고가 중간값 기준 추정, KAMIS 매칭 시 오늘 시세 병기. 중복·자기 자신 제안 방지.
  즉석밥→쌀은 이번 주 지출이 커져 예산 맥락에서 제외.
- UI: 경고 톤 카드(초과액·스왑·근거·추정치 고지). AI 스키마와 분리된 서버 부착(budget_fix).
- 무DB·무신규API·무키 폴백 유지. 쇼케이스 KAMIS 카드 문구를 구현 사실에 맞게 복원.

### C-2. 냉장고 사진 → 재료 추출 (2026-07-03)
- /api/grocery mode=extract — 기존 비전 파이프라인 재사용(callClaudeJson hasImage,
  /api/assist 와 동일한 이미지 서버측 상한·형식 검증). 프롬프트는 "확실한 것만·일반 명칭·
  최대 15개·추측 금지", validateGroceryExtract(빈 배열 허용 — 식재료 없는 사진).
- UI: '남은 재료' 탭 상단 PhotoUpload(라벨 props 추가 — 기본값 유지로 수리 탭 무변화).
  사진 선택 즉시 추출 → 기존 입력과 중복 제거 병합해 입력창에 채움(수정 가능).
  실패·빈 결과·네트워크 오류 전부 "직접 입력해주세요" 안내로 폴백 — 수동 입력이 항상 살아 있음.
- 검증: 성공(병합·중복 제거)·실패(폴백 안내·입력 유지)·형식 오류(400) Playwright/curl 확인.

---

## v10. 심사 완성도 종합 패스 (2026-07-03)

### P0 — 쇼케이스 v2 + 정보 구조 + 보안
- /showcase 전면 고도화: 시나리오 4종(수리·계약서·장보기·**시세 추가**) × 4단계,
  클릭 가능한 스테퍼, 단계별 스켈레톤→카드 진행, 폰 status bar, 단계 3에서 실제 기능 이동.
- 데이터 접지 10종 카드(보강 내용/무키 폴백/화면 표시·주의 3필드) — 실제 동작 기준만 서술.
- 발표자 모드: ?present=1 또는 버튼, 시나리오별 발표 멘트, 전체 진행바, ←→ 이동, Esc 종료.
- Impact 섹션(정성 메시지만 — 근거 없는 수치 금지 준수) + 막막함→행동 흐름도.
- 신뢰 배지 5종(TrustBadges) 쇼케이스·전체 탭 공용. 홈 히어로에 '글로만 적어도' 안내.
- 레이트리밋 버킷 분리(비전 더 엄격) + rent 프록시 보호, 보안 헤더 4종(CSP는 TODO),
  assist 로그에서 message 제거(사용자 원문 차단).

### P1 — 신뢰 UX
- 계약서: 위험도별 형광펜(레드/앰버/스카이 + 범례, 겹침 시 높은 위험 우선),
  위험 높은 순 정렬, 원문→수정 요청 diff 톤(레드/그린), 자주 문제되는 특약 칩 4종.
- 시세: 내 월세 비교 미터(중앙값 대비 낮음/비슷/높음, 0.5~1.5배 매핑) — 참고 비교 명시,
  이 기기에서만 계산, 예시 데이터일 땐 그 사실 표기, 색+텍스트 병행.
- 수리: 증거 기록 패키지 카드(사진·영상·시각·연락 기록) + 기록용 메모 양식 복사.

### P2 — 문서·메타
- docs/JUDGE_DEMO_SCRIPT.md: 90초 표, 클릭 순서, 예시 입력, 키 유/무 폴백 표, 예상 Q&A.
- /showcase OG(제목·설명), README 최신화(시세·쇼케이스·환경변수 표 전체).

### 하지 않은 것 (스펙의 선택 항목 중)
- 이미지 provider 다중화(Unsplash/TheMealDB): Pexels+폴백으로 충분, 새 키 의존 추가 회피.
- next/image + remotePatterns 전환: Pexels 이미지가 img+lazy+고정 비율로 이미 안정적,
  도메인 화이트리스트 유지보수 비용 대비 이득 낮다고 판단(레이아웃 시프트 없음 확인).
- extract confidence 필드: 프롬프트가 '확실한 것만'을 강제하므로 string[] 유지(스펙 허용).
- 데이터 소스 네트워크 그래픽·OG 이미지 생성: 시간 대비 임팩트 낮아 보류.

---

## v11. 잔여 격차 마감 (2026-07-03)

- 집주인 메시지: 표시 전용 → 수정 가능한 textarea (톤 전환 시 원문 복원, 저장 안 함 고지).
- 증거 기록: 체크박스화 + 결과 해시별 localStorage (NextSteps와 동일 패턴, 키 v1).
- 냉장고 사진: 스캔 오버레이(animate-scan, reduced-motion 정지) + 추출 칩 순차 등장·
  개별 삭제(입력창 동기화). "사진은 저장하지 않아요" 문구.
- 장보기 '리스트 복사'(코너별 □ 체크 텍스트 + 참고용 합계), 시세 '계약 전 확인 문구' 복사 카드.
- 홈 쇼케이스 카드 카피를 일반 사용자용("서비스 흐름 90초로 보기")으로, Judge 배지는 유지.
- 발표자 모드 Space 재생/일시정지.
- 문서: SECURITY_PRIVACY / API_FALLBACKS / DESIGN_SYSTEM 신설, JUDGE_WOW_NOTES 전면 갱신.
- npm audit: next@14 high 1(이미지 최적화·미들웨어·RSC advisory 묶음)·postcss moderate 1 —
  해결에 next@16 메이저 필요 → 마감 전 보류, 노출면 제한적(next/image·middleware 미사용).
  별도 브랜치에서 Next/PostCSS/ESLint 동시 검증 권장 (SECURITY_PRIVACY.md 기록).
- 컴포넌트 분리(showcase/grocery 등 디렉터리 재편)는 마감 전 회귀 위험 대비 이득이 낮아
  보류 — 기능 안정성 우선 원칙.

---

## v12. 홈 직관성 — 사용자 언어를 진입 경로로 (2026-07-03)

- 히어로에 증상·상황 칩 6개: 누수·곰팡이·보일러·벽균열(→ /repair?q=프리필),
  계약서(→ /contract), 월세(→ /rent). 라벨은 짧게, prefill은 자연문으로 분리.
  가로 스크롤 레일(-mx-5 브리드, 스크롤바 숨김), min-h 44px, aria-label, 톤 정적 매핑.
- AssistWorkspace ?q= 프리필: window.location.search 패턴 재사용(기존 ?h=와 동일,
  useSearchParams/Suspense 회피). ?h= 우선, repair 한정, 기존 입력 보존, 120자 컷,
  자동 제출·저장 없음.
- 서브카피를 기능 나열("곰팡이, 누수, 계약서, 공과금...")에서 가치 설명
  ("사진이나 한 줄 설명만 있어도...")으로 — 구체 문제명은 칩이 담당.
- 홈 순서: Hero → QuickActions → 생활도구 → NestGrowth → Recent → 쇼케이스(하단·
  카드 스타일로 축소, 다크 그라디언트 제거, "서비스 흐름"+"발표용 데모" 배지) → 안내.
  로드맵 카드는 제거하고 disclaimer 한 줄("준비중인 기능은 전체 탭")로 축소.
- QuickActions 질문형 카피: "계약서, 사인해도 될까요?" / "이 월세, 적정할까요?".
- 공과금 칩은 의도적으로 제외 — 수리·계약·월세 핵심 정체성 유지.

---

## v13. 홈 증상 버튼 — 스크롤 레일 → 2×2 그리드 (2026-07-03)

- 증상 칩 6→4개: 수리 증상(누수·곰팡이·보일러·벽균열)만 남기고 계약서·월세 칩 제거 —
  QuickActions 질문형 카드와 중복이었고, 칩의 역할을 '수리 진단 하위 진입'으로 좁힘.
- 가로 스크롤 레일 → grid grid-cols-2: 4개 버튼이 스크롤 없이 전부 보임(발견성),
  -mx 브리드 제거로 좌측 잘림·포커스 링 클리핑 원인 자체가 사라짐.
- 버튼 UI: min-h 48px, 흰 원형 아이콘 배지(size-7, strokeWidth 2.4 통일),
  bg-brand-tint/80 + 연한 브랜드 보더 + 은은한 브랜드 그림자, hover 시 화이트.
- 라벨 "내 상황으로" → "수리 증상으로 바로 시작해요" (역할 명확화).
- 검증: 360/390/430px 4버튼 전부 표시·가로 넘침 없음·프리필 회귀 통과.
  리뷰 워크플로(2렌즈+반박 검증) 발견 4건 전원 반박 — 실결함 0건.
- 후속: FeatureRow(생활 도구·전체 탭 공용) 아이콘 타일 rounded-xl → rounded-2xl —
  QuickActions와 동일 규격으로 통일 (44px 타일 = rounded-2xl, 인라인 배지 = 원형).

---

## v14. 홈 "핵심 기능" 섹션 재구성 (2026-07-03)

- 신규 CoreFeatures 컴포넌트: "핵심 기능" 제목 + 소개 + 3개 카드(집수리·계약서·시세)를
  사이에 끼는 콘텐츠 없이 연속 1열 그룹으로. 카드 = 아이콘 타일(h-12) + 굵은 기능명(16px,
  설명보다 먼저) + 한 줄 설명(말줄임 없이) + 화살표. 집수리에만 '메인' 배지.
- 기능명 중심 카피(질문형 폐기): "계약서, 사인해도 될까요?" → "계약서 위험 조항 점검" 등.
  화면 표기용 이름·설명은 HOME_CORE_FEATURES로 FEATURES와 분리(홈 전용 또렷한 카피).
- 히어로 축소: 주 CTA 버튼·수리 증상 그리드·"수리 증상으로 바로 시작해요" 라벨 제거
  → "수리 진단"이 섹션 제목처럼 보이던 문제 해소, '핵심 기능' 제목이 첫 화면(Y≈244)에 노출.
  집수리 진입은 핵심 기능 카드 #1(메인 배지)이 담당.
- 위계 분리: 핵심 기능 = 큰 개별 카드(그림자·큰 아이콘), 생활 도구 = 구분선 후 컴팩트
  리스트(FeatureRow). 생활 도구 4종 설명을 선언형 ~해요 톤으로 통일, FeatureRow는
  truncate → line-clamp-2로 두 줄 허용.
- QuickActions 삭제(계약서·월세가 핵심 기능으로 흡수). 계절 뱃지(장마철 곰팡이 주의)는
  이번 IA에 자리 없어 제거 — 필요 시 향후 핵심 카드에 재도입 검토.
- 라우트/링크/동작 불변. HOME_SYMPTOM_CHIPS 제거(미사용), AssistWorkspace ?q= 딥링크
  프리필 effect는 유지(무해·재사용 가능).

---

## v15. 에어컨 전기요금 계산기 (2026-07-13)

- **LLM이 아닌 순수 함수로 구현한 이유**: 같은 입력→같은 결과(결정론), 한전 요금표와의
  일치를 단위 테스트로 보증, API 비용·네트워크 무관 즉시 계산, LLM의 숫자 생성 위험 차단.
  계산 중 네트워크 요청 0회 (Playwright로 확인).
- **기존 /utility를 탭 구조로 확장한 이유**: 새 라우트 없이 전기요금 관심사를 한 곳에 모으고,
  기존 AI 공과금 점검·?h= 기록 링크를 그대로 보존(?h= 진입 시 고지서 탭 자동 선택).
  AssistWorkspace에 embedded 모드(main/AppBar/서브타이틀 생략)를 추가해 본문을 재사용.
- 구조: 공식 요금 스냅샷(tariffs.ts, kr-residential-2026-q3, 확인일 2026-07-13)과
  앱 휴리스틱(airconProfiles.ts)을 분리. 추가요금은 가정 전체 누진요금 전/후 차이.
  평소 사용량 미입력 시 150/200/250kWh 시나리오 → 낮음·중앙·높음 범위로 표시(단정 금지).
- 표시는 1,000원 단위 반올림("약 24,000원") — 엔진은 10원 단위 정확(한전식 절사·4사5입,
  정확값 테스트 6종 + 경계·슈퍼유저·불변식·단조성 29개 테스트, tsx + node:test).
- 홈 핵심 기능 4번째 카드로 승격(coral), 생활 도구 중복 제거.
- localStorage 복원 버그 수정: 저장 게이트를 ref → state(hydrated)로 — ref면 mount 직후
  저장 이펙트가 초기값으로 스토리지를 덮어써 StrictMode 이중 실행에서 복원값이 유실됨.
- 셀프 리뷰(적대적 검증) 반영: ① 시나리오 정렬 시 라벨 재배정 금지 — 기타계절에서 기본
  baseline 200kWh가 1단계 경계에 걸리면 central이 high보다 커질 수 있는데, 값을 서로
  바꾸면 central이 전/후 고지서 상세와 어긋난다 → central은 유지하고 low/high를 min/max로
  넓힘. ② 세그먼트·멀티모드 선택을 button role="radio" → 실제 radio input(sr-only)으로
  교체(네이티브 방향키·폼 시맨틱). ③ 숫자 필드 검증을 타이핑 중 → blur로 이동(min 100 필드는
  모든 정상 입력이 오류로 시작하는 문제), blur 시 clamp + role="status" 조정 안내. 이때
  onCommit이 되돌아오는 value 변경이 안내를 지우던 레이스를 strRef(자기 변경 구분)로 해결.
  ④ 누진 카드 라벨을 justify-between → 경계 위치(pct) 절대배치로 막대와 정렬.
  ⑤ ?h=/?tab= 초기 탭을 서버 searchParams에서 결정해 첫 페인트 깜빡임 제거(클라 폴백 유지).

---

## v16. 내 에어컨 찾기 — 라벨 사진·모델명으로 정확도 높이기 (2026-07-13)

- **선택 기능 원칙**: 패널을 열지 않아도, 판독·검색이 실패해도, 키가 없어도 종류·평수
  계산은 완전히 동작. 검색 키 미설정은 `configured:false`로 조용히 건너뛴다.
- **단정 금지 UX**: "라벨에서 OOO로 읽었어요"만 사용, "정확히 OOO 모델입니다" 금지.
  후보가 하나여도 자동 확정하지 않고 "내 에어컨이 맞아요" 클릭으로만 적용
  (rankCandidates는 선택 상태 필드 자체가 없음 — 테스트로 계약 고정).
- **냉방능력 W ≠ 냉방 소비전력 W**: 프롬프트에서 구분 지시 + 서버 sanitize에서
  소비전력은 100~5,000W 범위 밖이면 null(능력 오인 의심). 계산 override는
  ratedCoolingPowerW만. 상품 제목·가격·면적으로 소비전력 추측·역산 금지.
- **소비전력 출처 우선순위**(lib/aircon/power.ts): 라벨 사진(신뢰도≥0.7) > 직접 입력 >
  제조사 > 검색 교차 확인 > 평수 추정. 결과 카드에 "제품 라벨 650W 반영" 식으로 출처 표기,
  모델만 확인되고 소비전력 미확인이면 그 사실을 명시(사진 일치 ≠ 소비전력 검증).
- **프라이버시**: 사진은 브라우저에서 긴 변 1800px 재인코딩(EXIF 제거) 후 분석에만 사용,
  서버 로그에 이미지 내용 미출력, 시리얼·제조번호는 프롬프트 금지 + scrubSerial로
  반환 데이터에서 마스킹. 네이버 키는 서버 라우트 환경변수로만.
- **검색 점수화**: 완전 일치 +100 / 정규화 일치 +90 / 제조사 +30 / "에어컨" +20 /
  유형 +15 / 공식몰 +10 / 액세서리 -50 / 렌탈·중고 -30 / 부분 일치 -25, 상위 3개.
  쇼핑 결과가 없을 때만 이미지 검색 fallback. 실패는 전부 빈 후보로 수렴.
- 셀프 리뷰(적대적 검증, 23건 중 15건 확정) 반영: ① scrubSerial 재설계 — "Serial No.:",
  "S.N.", "제조번호(S/N):" 우회와 SN 접두 모델번호(SNC-5000W) 파괴를 동시에 해결
  (마커\b + 비영숫자 0~8자 구분자). ② 능력→소비전력 교차 가드 실제 구현 — 소비전력 ≥
  냉방능력이면 능력 오인으로 null + 경고(주석만 있던 가드). ③ 완전 일치 판정을 제목
  전체 includes → 토큰 결합 등호 비교로 교체(접두어 AR07T917 ⊂ AR07T9170HZS 오판 제거).
  ④ detectProductType에서 multi 우선 검사(2in1 제목의 벽걸이/스탠드 오분류).
  ⑤ extractModelToken: 정규화 6자 미만·마케팅 토큰(WiFi2024) 제외, 최장 토큰 선택.
  ⑥ identify 라우트: Content-Length 신고값 대신 본문 실측 길이 검사. ⑦ 복원 시
  product.productType 화이트리스트(손상 저장값 렌더 크래시 방지) + v15 저장값의 labelW는
  user-input 출처로 이행. ⑧ 재검색 시 searched 리셋(빈 결과 오표시), powerNote는 적용
  상태와 동기 표시, 업로드 reading 게이트(동시 업로드 경합), 이미지 실패 시 항상 유형
  아이콘. ⑨ 제조사·교차확인 출처는 데이터 공급 경로가 없어 미발생임을 문서에 명시.
  회귀 테스트 5건 추가(55개).
